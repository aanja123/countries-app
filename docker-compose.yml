services:
  backend:
    build: ./countries-backend
    ports:
      - "8080:8080"
    environment:
      REST_COUNTRIES_API_KEY: ${REST_COUNTRIES_API_KEY}

  frontend:
    build: ./countries-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend